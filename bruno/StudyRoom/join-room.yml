info:
  name: join-room
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3038/api/rooms/join
  body:
    type: json
    data: |-
      {
        "joinCode": "C7B85"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
