info:
  name: create-user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3038/api/users
  body:
    type: json
    data: |
      {
        "email": "bpn.nava@gmail.com",
        "passwordHash": "mipassword5678",
        "displayName": "Brian"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
