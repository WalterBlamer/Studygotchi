info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3038/api/login
  body:
    type: json
    data: |
      {
        "email": "bpn.nava@gmail.com",
        "password": "mipassword5678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
