info:
  name: create-event
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3038/api/events
  body:
    type: json
    data: |-
      {
        "text": "Presentation",
        "eventDate": "2026-05-10T15:00:00Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
