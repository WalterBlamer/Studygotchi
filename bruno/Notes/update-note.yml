info:
  name: update-note
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:3038/api/notes/:noteId
  params:
    - name: noteId
      value: 019dfeba-a8b0-7223-a4b0-895b99dfff03
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Project 1",
        "text": "working on bruno endpoints, updated note"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
