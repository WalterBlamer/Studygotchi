info:
  name: create-note
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3038/api/notes
  body:
    type: json
    data: |
      {
        "title": "Project",
        "text": "Frontend needs pet development and design"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
