info:
  name: get-all-notes
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3038/api/notes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
