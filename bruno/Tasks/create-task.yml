info:
  name: create-task
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3038/api/tasks
  body:
    type: json
    data: |-
      {
        "title": "Write paper",
        "text": "finsih last two pages of philosophy paper"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
