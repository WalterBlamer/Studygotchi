info:
  name: get-tasks
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3038/api/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
