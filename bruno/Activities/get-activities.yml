info:
  name: get-activities
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3038/api/activities
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
